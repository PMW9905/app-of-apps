apiVersion: v1
kind: Service
metadata:
  name: fish-bowl
  namespace: minecraft
spec:
  selector:
    app: fish-bowl
  ports:
    - port: 25565
      targetPort: 25565
      protocol: TCP
  type: ClusterIP
